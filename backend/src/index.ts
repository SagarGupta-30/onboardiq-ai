import app from './app';

const PORT = process.env.PORT || 5050;

app.listen(PORT, () => {
  console.log(`=========================================`);
  console.log(`🚀 OnboardIQ AI API Running on port ${PORT}`);
  console.log(`🔒 Security posture scanner initialized`);
  console.log(`🤖 AI assistant orchestration layer active`);
  console.log(`=========================================`);
});
